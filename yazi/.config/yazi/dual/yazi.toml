# Конфигурация yazi для dual-pane режима (команда yd)
# Используется только ratio — остальные настройки берутся из основного конфига

[mgr]
linemode = "size"
ratio = [0, 1, 0]  # Только текущая директория — без parent и preview панелей
