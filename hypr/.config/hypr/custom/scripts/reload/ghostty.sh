#!/usr/bin/env bash

# Ghostty config reload usually requires keybind (Ctrl+Shift+,) or restart.
notify-send -h string:x-canonical-private-synchronous:sys-notify "Ghostty" "To reload active window: Ctrl+Shift+,"
