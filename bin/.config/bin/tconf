#!/usr/bin/env bun
import { launchSession, type SessionBlueprint } from "./session-launcher";

const BASE_PATH = "~/Hellkitchen/dotfile";

// Декларативное описание сессии
const configBlueprint: SessionBlueprint = {
  sessionName: "config",
  windows: [
    {
      name: " nvim",
      dir: `${BASE_PATH}/nvim/.config/nvim`,
      cmd: "clear",
      focus: true,
    },
    { name: " hypr", dir: `${BASE_PATH}/hypr/.config/hypr`, cmd: "clear" },
    {
      name: " waybar",
      dir: `${BASE_PATH}/waybar/.config/waybar`,
      cmd: "clear",
    },
    { name: "󰒋 tmux", dir: `${BASE_PATH}/tmux/.config/tmux`, cmd: "clear" },
    { name: " kitty", dir: `${BASE_PATH}/kitty/.config/kitty`, cmd: "clear" },
    { name: " fish", dir: `${BASE_PATH}/fish/.config/fish`, cmd: "clear" },
    { name: " yazi", dir: `${BASE_PATH}/yazi/.config/yazi`, cmd: "clear" },
    { name: "󰨭 rofi", dir: `${BASE_PATH}/rofi/.config/rofi`, cmd: "clear" },
    { name: " bin", dir: `${BASE_PATH}/bin/.config/bin`, cmd: "clear" },
    { name: " claude", dir: `${BASE_PATH}/claude/.claude`, cmd: "clear" },
    {
      name: " startup",
      dir: `${BASE_PATH}/startup/.config/startup`,
      cmd: "clear",
    },
    { name: "󰝡 other", dir: `${BASE_PATH}/other/.config/other`, cmd: "clear" },
  ],
};

// Запуск движка
await launchSession(configBlueprint);
